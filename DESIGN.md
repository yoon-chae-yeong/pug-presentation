# 🐶 AI Power User Group (PUG) 디자인 가이드라인

본 가이드는 AI Power User Group(PUG)의 공식 발표자료 및 시각 자료 제작을 위한 기준입니다. 퍼그(Pug)의 친근하고 따뜻한 무드를 유지하되, **명료함(Clarity)** 을 최우선으로 합니다. 그림자·그라데이션 같은 입체 가공을 배제하고, **흰 카드 · 또렷한 잉크 텍스트 · 절제된 옐로우 포인트**의 깔끔한 대비로 정보가 한눈에 들어오는 **'웜 클린(Warm Clean) 미니멀리즘'** 스타일을 지향합니다.

---

## 1. 핵심 디자인 원칙
1. **Clarity First (명료함 우선)**: 톤이 비슷한 색을 겹쳐 화면이 탁해지지 않도록 합니다. 배경·면·텍스트는 **명도 차이를 분명히** 두어 요소가 서로 또렷하게 분리되어야 합니다.
2. **White Cards on Warm Page**: 콘텐츠는 **순백(또는 거의 흰) 카드** 위에 담고, 페이지 배경은 살짝 따뜻한 오프화이트를 씁니다. 이 명도 대비만으로 영역을 구분하며, 테두리 선은 쓰지 않습니다.
3. **No Gradients, No Shadows**: 모든 면은 단색(Solid)입니다. 그라데이션·그림자·블러는 사용하지 않습니다.
4. **Yellow is an Accent, not a Surface**: 옐로우는 큰 면적을 채우는 색이 아니라, **핵심 1~2곳을 콕 집는 포인트**입니다. 넓게 칠하면 화면이 누렇고 탁해집니다.

---

## 2. 컬러 시스템
명도 대비를 분명히 하기 위해 아래 토큰만 사용합니다. 임의의 중간색·탁한 베이지를 새로 만들지 않습니다.

| 역할 | 이름 | HEX | 용도 |
| :--- | :--- | :--- | :--- |
| **페이지 배경** | Warm Paper | `#FBF8F3` | 화면 전체 바탕. 따뜻하지만 거의 흰색에 가까워 카드가 또렷이 떠 보입니다. |
| **카드 면** | Pure White | `#FFFFFF` | 모든 콘텐츠 카드·패널의 기본 면. 흰 카드가 따뜻한 배경 위에서 깔끔히 분리됩니다. |
| **잉크(텍스트/구조)** | Espresso Ink | `#33271F` | 모든 본문·제목·아이콘·차트 선. **충분히 진한 에스프레소 브라운**으로 흰 면 위에서 선명하게 읽힙니다. |
| **보조 텍스트/구분** | Soft Taupe | `#8A7B6E` | 부연 설명, 라벨 등 2차 정보. 잉크보다 옅어 위계를 만듭니다. |
| **연한 면/트랙** | Cloud Beige | `#F0EAE0` | 진행바 트랙, 입력창, 아주 옅은 강조 면 등 보조 배경. |
| **포인트** | Pug Yellow | `#F5B041` | 핵심 강조 1~2곳(주요 버튼, 최종 지표 막대, 선택 상태)에만 제한적으로 사용. |

**대비 규칙**
- 본문 텍스트는 항상 `Espresso Ink`를 흰/연한 면 위에 올립니다. (옅은 브라운을 본문에 쓰지 않습니다 → 탁함의 원인)
- 옐로우 면 위 글자는 반드시 `Espresso Ink`로 반전합니다. (옐로우 위 흰 글자 금지 — 안 보입니다.)
- 옐로우와 옅은 베이지를 바로 인접해 큰 면적으로 붙이지 않습니다.

---

## 3. 타이포그래피 및 위계 (3단계 제한)
글자 크기·굵기는 3단계로만 구분합니다.

| 위계 | 권장 크기 | 적용 대상 | 규칙 |
| :--- | :--- | :--- | :--- |
| **Level 1 (대타이틀)** | 1.5~2.0rem | 화면 제목, 핵심 헤드라인 | `Espresso Ink`, Bold |
| **Level 2 (서브타이틀)** | 1.0~1.15rem | 카드 제목, 소제목 | `Espresso Ink`, Semibold |
| **Level 3 (본문/상세)** | 0.75~0.85rem | 상세 설명, 데이터 지표 | 본문은 `Espresso Ink`, 부연은 `Soft Taupe` |

---

## 4. 레이아웃 및 컴포넌트 규칙

### 4.1. 카드 & 면 사용
*   콘텐츠는 `Pure White` 카드에 담고 `Warm Paper` 배경 위에 배치합니다. 카드와 배경의 명도 차이로 구분하며 테두리 선은 두르지 않습니다.
*   카드 안에서 한 번 더 구분이 필요하면 `Cloud Beige`를 옅게 깝니다. 흰 위에 흰을 또 겹쳐 헷갈리게 하지 않습니다.
*   어두운(잉크) 채움 블록은 **화면당 1개 이내**로 최소화합니다. 강조가 꼭 필요한 단일 히어로 영역에만 허용합니다.

### 4.2. 포인트(옐로우) 사용
*   옐로우는 화면당 핵심 강조 **1~2곳**에만. 예: 주요 CTA 버튼, 선택된 항목, '최종(After)' 지표 막대.
*   로고/시그니처 블록처럼 작은 면에 옐로우를 깔고 그 위에 잉크 글자를 반전해 얹는 방식은 권장합니다.

### 4.3. 지표 및 인포그래픽
*   격자선·축선은 아주 옅게(잉크 10~12% 정도) 최소한만 그립니다.
*   데이터 비교는 **`Espresso Ink` 면(Before/기준)** 과 **`Pug Yellow` 면(After/강조)** 의 크기 차이로만 표현합니다.
*   진행바 트랙은 `Cloud Beige`, 채움은 기준값=잉크 / 강조값=옐로우로 구분합니다.

### 4.4. 모서리 & 형태 (절제된 라운딩)
*   과도하게 둥근 모서리를 금지합니다. 정돈된 인상을 위해 라운딩은 아래 2단계만 사용합니다.
    *   **카드·패널·버튼·입력창**: `8px` 내외 (Tailwind `rounded-lg`). 큰 컨테이너는 이 값을 넘지 않습니다.
    *   **작은 요소(배지·토글·진행바·점)**: 원형/알약(`rounded-full`) 허용.
*   `rounded-2xl`, `rounded-3xl` 등 12px 초과 라운딩은 사용하지 않습니다.

---

## 5. 잘못된 예시 (Don'ts)
*   ❌ 배경·카드·포인트를 모두 비슷한 베이지/옐로우 톤으로 깔아 화면이 탁해지게 하기
*   ❌ 본문 텍스트에 옅은 브라운(`Soft Taupe`)을 써서 대비가 약해 안 읽히게 하기
*   ❌ 옐로우를 카드 전체 등 큰 면적에 채우기 (포인트는 콕 집는 용도)
*   ❌ 옐로우 면 위에 흰 글자 얹기 / 요소 테두리에 진한 외곽선 두르기
*   ❌ 카드나 버튼 배경에 그림자·그라데이션 넣기
*   ❌ 위계를 위해 4개 이상의 글자 크기 섞어 쓰기
